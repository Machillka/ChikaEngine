# Step 3.6 - Parallel CPU Renderer Scaling Gate

## Metadata

- Status: Planned
- Depends on: Step 3.5
- Estimate: 1 day

## Goal

验证串行和 Job CPU 路径正确性、低负载代价及 1/2/4/8 Worker 扩展性。

## Responsibility

- Benchmark 运行对照实验。
- Renderer 提供 Serial/Jobs 显式模式。
- 文档记录结果与未扩展原因。

## Path Plan

- `RenderSettings.hpp`
- `BenchmarkOptions.hpp`
- `docs/dev/results/cpu-renderer/`
- `tests/integration/RenderModeEquivalenceTests.cpp`

## Implementation

- 固定场景/config hash 下运行 Serial 与 Jobs。
- 输出各 Zone、Worker utilization、queue wait、steal。
- 记录 1K 调度开销和 10K/50K 收益。
- 自动比较 Visible/Packet/Batch Hash。

## Unit Tests

- 模式切换不改变结果。
- Jobs 不可用时显式回退并记录原因。
- 多次运行 Hash 一致。
- Validation run 无错误。

## Delivery Checklist

- [ ] 保存全部原始 JSON。
- [ ] 绘制 Worker scaling 曲线。
- [ ] 报告 p50/p95/p99。
- [ ] 解释低对象数阈值。

## Boundaries

- 不隐藏负收益配置。
- 不调整场景以迎合结果。
- 不开始 GPU 路径直到 Gate 通过。

## Gate

并行结果与 Oracle 一致，并能用 Profiler 解释扩展上限或收益不足。
