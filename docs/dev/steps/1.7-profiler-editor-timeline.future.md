# Step 1.7 - Editor Profiler Timeline

## Metadata

- Status: Complete (2026-06-19)
- Depends on: Step 1.6
- Estimate: 2 days

## Goal

实现只读 Timeline、Frame History、Thread Lanes、GPU Lane 和 Zone Details。

## Responsibility

- Editor Panel 只消费不可变 Capture。
- Timeline Model 负责缩放、筛选和布局，可脱离 ImGui 测试。
- Runtime Profiler 不依赖 Editor。

## Path Plan

- `engine/Editor/include/ProfilerTimelinePanel.hpp`
- `engine/Editor/src/ProfilerTimelinePanel.cpp`
- `engine/Editor/include/ProfilerTimelineModel.hpp`
- `engine/Editor/src/ProfilerTimelineModel.cpp`
- `tests/unit/ProfilerTimelineModelTests.cpp`

## Implementation

- 最近帧图、pinned/hitch 标识。
- Main/Worker/GPU lanes。
- Pan/Zoom、Zone hover/select、inclusive/exclusive details。
- Top Hotspots 和 dropped/malformed 警告。

## Unit Tests

- 时间到像素转换、裁剪和缩放稳定。
- 同线程嵌套 Zone lane depth 正确。
- 空 Capture、超长名称和大量 Zone 不崩溃。
- Panel 关闭不停止 Runtime 采集。

## Delivery Checklist

- [x] 300 帧浏览可用。
- [x] CPU/GPU 通过同一 Capture Frame ID 展示。
- [x] UI 不持有 Runtime 可变指针。
- [x] Timeline Model 先裁剪再生成绘制矩形。

## Boundaries

- 不修改 Renderer 设置。
- 不实现远程连接。
- 不在 OnImGuiRender 中聚合原始事件。

## Next

- Step 1.8：Trace 导出与开销 Gate。

## Implementation Record

- Profiler Panel 提供 capture 开关、最近帧图、p50/p95/p99、历史选择、pin、pan/zoom、CPU thread lanes、GPU lane、hover 时长、hotspots 与 dropped/malformed 警告。
- TimelineModel 独立完成时间到像素、视区裁剪和 exclusive hotspot 聚合，不依赖 ImGui。

## Changed Files and Reasons

- `ProfilerTimelinePanel.hpp/.cpp`：只读 UI 和 Perfetto 导出入口。
- `ProfilerTimelineModel.hpp/.cpp`：可测试的布局/统计模型。
- `EditorManager.cpp`：注册 Profiler 面板。

## Knowledge and Design Notes

- Panel 只保存 `shared_ptr<const Capture>`，原始 ring buffer 永远不暴露给 UI。
- 大 Capture 的首要控制是视区裁剪；后续若事件达到百万级，再增加层级 LOD/聚合缓存，而不是让 Runtime 为 UI 改变采集格式。

## Verification Record

- Model 单测覆盖左右裁剪、像素映射、聚合与 hotspot 排序；`ChikaEditor` 构建通过。
