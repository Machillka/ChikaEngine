# Step 3.3 - Parallel CPU Visibility

## Metadata

- Status: Planned
- Depends on: Step 3.2, Step 2.6
- Estimate: 1-2 days

## Goal

使用 ParallelFor 分块执行 Frustum/Layer/Flag 测试，并确定性合并 Visible Set。

## Responsibility

- Visibility Task 只读取 RenderSceneView 和 RenderView。
- 每 Chunk 独占输出。
- 主线程/Continuation 按 chunkIndex 合并。

## Path Plan

- `RenderVisibility.hpp/.cpp`
- `ParallelRenderVisibility.hpp/.cpp`
- `tests/unit/ParallelVisibilityTests.cpp`
- `tests/performance/VisibilityBenchmarks.cpp`

## Implementation

- 保留 `BuildVisibilitySerial`。
- 新增 `BuildVisibilityParallel(JobSystem&, ...)`。
- 小对象数按阈值串行回退。
- Counter 合并 tested/visible/culled。

## Unit Tests

- 空、单对象、边界 AABB、无效 Bounds。
- 1/2/4/8 Worker 与串行 Visible Hash 一致。
- Shadow caster 和 layer mask 结果一致。
- Worker 异常不会留下半完成结果。

## Delivery Checklist

- [ ] 无共享 output push mutex。
- [ ] Profiler 显示 Visibility chunks。
- [ ] Grain size 可配置且有默认值。
- [ ] 低对象数自动回退。

## Boundaries

- 不并行 Packet 构建。
- 不修改 Frustum 数学。
- 不访问 ResourceManager。

## Next

- Step 3.4：并行 Packet Generation。

