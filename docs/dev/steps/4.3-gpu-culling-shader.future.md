# Step 4.3 - Reflected Compute Culling Pipeline

## Metadata

- Status: Planned
- Depends on: Step 4.2
- Estimate: 1-2 days

## Goal

新增最小 Compute Shader 和 Reflection 驱动的 Compute Pipeline，首版只验证读取 Instance 和写 Visibility Flags。

## Responsibility

- Shader Importer 编译/反射 Compute Shader。
- ResourceManager 基于 Shader Interface 创建 Compute Pipeline。
- Renderer 不手写 set/binding。

## Path Plan

- `Assets/Shaders/gpu_frustum_cull.comp`
- 对应 `.meta`
- `AssetLayouts.hpp`
- `ResourceManager.hpp/.cpp`
- `ShaderInterfaceTests.cpp`
- `tests/unit/GpuCullingPipelineTests.cpp`

## Implementation

- set 0 frame/view，set 2 instance/object output，遵守现有频率约定。
- 输入 instance/bounds/frustum/objectCount。
- 输出每 Instance 0/1 visibility flag。
- Compute Pipeline cache key 包含 reflected interface hash。

## Unit Tests

- Reflection 识别 Storage Buffer 和 Push Constant/UBO。
- C++ 不出现固定 binding number。
- Pipeline 在 Mock RHI 创建描述正确。
- 不兼容 Shader Interface 导入失败。

## Delivery Checklist

- [ ] Shader 自动编译和 sidecar 生成。
- [ ] Pipeline 使用 Reflection。
- [ ] Debug Name 可读。
- [ ] Shader Interface 测试通过。

## Boundaries

- 不接入 RenderGraph。
- 不生成 Indirect Args。
- 不做 Hi-Z。

## Next

- Step 4.4：RenderGraph GPU Buffer 流。

