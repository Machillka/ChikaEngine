# Step 4.8 - GPU-driven Performance and Correctness Gate

## Metadata

- Status: Planned
- Depends on: Step 4.7
- Estimate: 1-2 days

## Goal

完成 Serial CPU、Job CPU、GPU-driven 三路径的统一正确性与性能对照。

## Responsibility

- Benchmark 运行实验并保存数据。
- Profiler 提供 CPU/GPU 因果证据。
- 报告说明适用范围和回退阈值。

## Path Plan

- `docs/dev/results/gpu-driven/`
- `engine/Benchmark/` mode matrix
- `tests/integration/RenderPathEquivalenceTests.cpp`
- `docs/dev/results/gpu-driven/gpu-driven-report.md`

## Implementation

- 1K/10K/50K/100K 三路径对比。
- 记录 Render Preparation、Compute Cull、Graphics、Draw/Indirect、upload bytes。
- 低对象数自动策略只在数据支持后加入。
- 正确性模式执行 Oracle；性能模式关闭 readback/validation。

## Unit Tests

- 固定场景三路径 Hash/count 一致。
- Scene/resize/mode hot switch 正确。
- Vulkan Validation 多帧无错误。
- GPU 不支持时 Benchmark 明确 skip/fallback。

## Delivery Checklist

- [ ] 保存原始 JSON 和硬件信息。
- [ ] 报告 p50/p95/p99。
- [ ] 显示 CPU 与 GPU 各自瓶颈。
- [ ] 不隐藏 1K 场景固定开销。

## Boundaries

- 不开始 Stretch 直到本 Gate 完成。
- 不把不同画质/分辨率结果直接比较。
- 不宣称未测设备兼容性。

## Gate

GPU-driven 路径正确可回退，并能用数据说明它在哪些规模优于 CPU 路径。
