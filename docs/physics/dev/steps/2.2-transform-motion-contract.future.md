# Step 2.2 - Transform and Motion Contract

## Metadata

- Status: Planned
- Priority: Core
- Depends on: Step 2.1
- Estimate: 3-4 days

## Goal

明确 Static、Kinematic、Dynamic 的 Transform authority，补齐常见刚体运动方法，并消除每步全量回读所有 Body 的低效同步。

## Authority rules

| Motion type | Authoritative source | Engine behavior |
| --- | --- | --- |
| Static | Transform before simulation | Transform 改变时重建或显式更新静态 Body；运行时频繁移动应改 Kinematic |
| Kinematic | gameplay target | 每 fixed step 使用 `MoveKinematic`，不从 physics 覆盖 gameplay target |
| Dynamic | physics simulation | 模拟后回写 Transform；直接改 Transform 必须走 `Teleport` |

## Planned API

- `Set/GetLinearVelocity`
- `Set/GetAngularVelocity`
- `AddForce`、`AddTorque`：按 fixed step 持续作用。
- `AddImpulse`、`AddAngularImpulse`：瞬时作用。
- `MoveKinematic(target, fixedDeltaTime)`
- `Teleport(position, rotation, resetVelocity)`
- `WakeUp`、`Sleep`、`IsSleeping`
- linear/angular damping、gravity factor、axis locks。

## Planned modules

- `Rigidbody`
- `PhysicsSubsystem` PreStep/PostStep
- `PhysicsScene` command and active-body snapshot
- `IPhysicsBackend` motion API
- `PhysicsJoltBackend`
- Transform dirty integration
- render interpolation state
- `tests/integration/PhysicsMotionTests.cpp`

## Implementation checklist

- [ ] backend 只返回 active Dynamic Body 的 transform snapshot，不遍历并锁定所有 body。
- [ ] Transform dirty 根据 motion type 生成 static update、kinematic target 或明确拒绝 dynamic direct write。
- [ ] Dynamic Teleport 定义 wake 和 velocity reset policy。
- [ ] Force 与 Impulse 分开，API 名称和单位在注释中明确。
- [ ] getter 使用上一步主线程 snapshot 或安全 backend read，不在任意线程直接访问 Jolt Body。
- [ ] 保存 previous/current physics transform，并提供 0..1 interpolation alpha 给 render-facing transform。
- [ ] parented physics object 的 world/local transform 转换有单独规则；不允许 parent non-uniform scale 静默破坏 shape。
- [ ] fixed step 被 clamp 时记录 dropped physics time/catch-up metric。

## Acceptance criteria

- 30/60/144 FPS 输入下，相同 fixed step 的 Dynamic 轨迹在容差内一致。
- Kinematic Body 按 target 移动并正确推动 Dynamic Body。
- Dynamic direct Transform edit 不产生一帧瞬移后又被物理覆盖的抖动。
- Interpolation 只影响显示，不反馈到物理 world。
- Sleeping Dynamic Body 不进入常规 transform upload，Wake 后恢复。
- Force、Impulse、Torque 和 axis lock 有数值测试。

## Verification

- `cmake --build build --target ChikaPhysicsMotionTests ChikaCoreBoundaryTests`
- `ctest --test-dir build --output-on-failure -R "Chika.PhysicsMotion|Chika.CoreBoundary"`
- 多帧率 oracle、parent transform、teleport 和 sleep/wake case。

## Risks

- 当前 Transform 同时服务场景层级和渲染；插值状态不能污染序列化或 authoring Transform。
- Kinematic API 必须携带 fixedDeltaTime，否则后端无法正确计算速度与接触响应。

## Boundaries

- 不在本步骤实现网络 prediction/rollback。
- 不把固定步长 accumulator 移入 backend；Scene 继续拥有 gameplay fixed-step orchestration。

## Next

Step 3.1：Collision Layers and Profiles。

