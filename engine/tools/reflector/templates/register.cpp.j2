// Auto-generated by ChikaEngine Registry Generator. DO NOT EDIT.
#include "debug/log_macros.h"

namespace ChikaEngine::Reflection
{
    // 1. 外部函数声明 (Anchor Functions)
    // 这些是定义在各个 .gen.cpp 中的 extern "C" 空函数
    extern "C" 
    {
    {% for func in anchor_functions %}
        void {{ func }}();
    {% endfor %}
    }

    // 2. 总初始化入口
    void InitAllReflection()
    {
        LOG_INFO("Reflection", "Initializing reflection registry...");
        
        // 3. 显式调用锚点函数
        // 这会强制链接器(Linker)将对应的 .obj 文件包含在最终的可执行文件中
        // 从而触发该文件内部的 static 变量初始化，完成反射自动注册。
    {% for func in anchor_functions %}
        {{ func }}();
    {% endfor %}

        LOG_INFO("Reflection", "All reflection units linked.");
    }

} // namespace ChikaEngine::Reflection