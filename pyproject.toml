[project]
name = "chikaengine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "clang>=21.1.7",
    "jinja2>=3.1.6",
    "pybind11>=3.0.2",
]

[tool.uv]
managed = true
